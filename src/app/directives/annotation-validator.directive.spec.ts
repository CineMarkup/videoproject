import { AnnotationValidatorDirective } from './annotation-validator.directive';

describe('AnnotationValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AnnotationValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
